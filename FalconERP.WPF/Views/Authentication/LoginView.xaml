<Window x:Class="FalconERP.WPF.Views.Authentication.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Falcon ERP Login"
        Height="350"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Falcon ERP"
                   FontSize="28"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1">

            <TextBlock Text="Username"/>

            <TextBox Height="35"
                     Margin="0,5,0,15"
                     Text="{Binding Username}"/>

        </StackPanel>

        <StackPanel Grid.Row="2">

            <TextBlock Text="Password"/>

            <TextBox Height="35"
                     Margin="0,5,0,15"
                     Text="{Binding Password}"/>

        </StackPanel>

        <Button Grid.Row="3"
                Content="Login"
                Height="40"
                Width="120"
                HorizontalAlignment="Center"
                Command="{Binding LoginCommand}"/>

    </Grid>

</Window>