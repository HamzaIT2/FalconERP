<UserControl x:Class="FalconERP.WPF.Views.Reports.CustomerReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->

        <TextBlock Text="تقرير العملاء"
                   FontSize="28"
                   FontWeight="Bold"/>

        <!-- الإحصائيات -->

        <UniformGrid Grid.Row="1"
                     Columns="2"
                     Margin="0,20,0,15">

            <Border Margin="5"
                    Padding="15"
                    BorderBrush="#DDDDDD"
                    BorderThickness="1"
                    CornerRadius="8">

                <StackPanel>

                    <TextBlock Text="عدد العملاء"
                               FontWeight="Bold"/>

                    <TextBlock Text="{Binding TotalCustomers}"
                               FontSize="24"/>

                </StackPanel>

            </Border>

            <Border Margin="5"
                    Padding="15"
                    BorderBrush="#DDDDDD"
                    BorderThickness="1"
                    CornerRadius="8">

                <StackPanel>

                    <TextBlock Text="إجمالي الديون"

                               FontWeight="Bold"/>

                    <TextBlock Text="{Binding TotalBalance}"
                               FontSize="24"/>

                </StackPanel>

            </Border>

        </UniformGrid>

        <!-- الجدول -->

        <DataGrid Grid.Row="2"
                  Margin="0,10,0,0"
                  ItemsSource="{Binding Customers}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="الاسم"
                                    Binding="{Binding Name}"
                                    Width="220"/>

                <DataGridTextColumn Header="الهاتف"
                                    Binding="{Binding Phone}"
                                    Width="180"/>

                <DataGridTextColumn Header="العنوان"
                                    Binding="{Binding Address}"
                                    Width="220"/>

                <DataGridTextColumn Header="الرصيد"
                                    Binding="{Binding Balance}"
                                    Width="150"/>

                <DataGridTextColumn Header="ملاحظات"
                                    Binding="{Binding Notes}"
                                    Width="250"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>