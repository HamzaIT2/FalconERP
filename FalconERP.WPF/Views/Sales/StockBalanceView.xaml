<UserControl x:Class="FalconERP.WPF.Views.Sales.StockBalanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="أرصدة المخزون"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Balances}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">

            <DataGrid.Columns>

                <DataGridTextColumn Header="اسم المنتج"
                                    Binding="{Binding ProductName}"
                                    Width="250"/>

                <DataGridTextColumn Header="إجمالي الإدخال"
                                    Binding="{Binding TotalIn}"
                                    Width="150"/>

                <DataGridTextColumn Header="إجمالي الإخراج"
                                    Binding="{Binding TotalOut}"
                                    Width="150"/>

                <DataGridTextColumn Header="الرصيد الحالي"
                                    Binding="{Binding Balance}"
                                    Width="150"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>